consent_form_ui <- function(id){
  ns <- NS(id)
  tagList()
}

consent_form_server <- function(id, parent.session){
  moduleServer(
    id,
    function(input, output, server){
      
      
    }
  )
}